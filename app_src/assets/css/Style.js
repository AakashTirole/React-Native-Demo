import React from 'react';
import {
    StyleSheet
  } from 'react-native';

  const Style = (props)  => {
      
  }
  const cssStyles = StyleSheet.create({
    h1: {
      display: 'flex',
      alignContent: 'center',
      justifyContent: 'center',
      minHeight: 250,
      backgroundColor: '#000'
    },
  });

  export default Style;